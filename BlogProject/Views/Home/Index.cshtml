@using BlogProject.Models
@model HomePageModel
@{
    ViewBag.Title = "Home Page";
}
<div class="main row">
    <div class="side-menu jumbotron col-md-4">
        @*<div class="tags-menu ">
            <strong>Popular Tags</strong>
            <ol>
                @foreach (var tag in Model.TagCount.Keys)
                {
                    <li>@tag</li>
                }
                <li>New</li>
                <li>.NET</li>
                <li>something else</li>
                <li>something else</li>
                
            </ol>

            <a href="/Tags" class="btn btn-info">Show All</a>
        </div>
        <hr style="border:1px gainsboro solid "/>*@
        <div class="post-menu ">
            
            <strong>Recent Posts</strong>
            <ol>
                @foreach (var post in Model.Post)
                {
                    <li><a href="/Posts/Details/@post.Id">@post.Title</a></li>
                }

            </ol>

            <a href="/Posts" class="btn btn-info">Show All</a>
        </div>
    </div>
    <section > 
        
            @foreach(var post in Model.Post.Take(3))
            { 
        <article class="jumbotron post row col-md-8">
            <h2><a href="/Posts/Details/@post.Id">@post.Title</a></h2>
            <div class="about">Posted on <i>@post.Date</i> by <i>@post.Author.FullName</i></div>
            <hr style="border:1px gainsboro solid " />
            <div class="body">@Html.Raw(post.Body)</div>
        </article>
            }

    </section>
</div>